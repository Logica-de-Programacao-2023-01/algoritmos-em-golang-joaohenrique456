package main

import "fmt"

func main() {
	var preco float64
	fmt.Print("Informe o valor do produto:")
	fmt.Scan(&preco)

	desconto := preco - preco*0.1
	fmt.Println("O preço do produto com desconto será", desconto)
}
